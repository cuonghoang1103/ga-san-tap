# Chuong 14.2 — pull_request_target nam o nhanh KHONG phai nhanh mac dinh.
name: ch14-prt
on:
  pull_request_target:
    branches: [ch14-chat-luong]
permissions:
  contents: read
jobs:
  nhin:
    runs-on: ubuntu-24.04
    steps:
      - env:
          WREF: ${{ github.workflow_ref }}
        run: echo "pull_request_target dang chay tu $WREF"
